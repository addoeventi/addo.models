{
  "name": "addo.models",
  "version": "1.0.1",
  "description": "modello dati",
  "main": "./dist/model.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "git-push": "tsc && git push origin master"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/addoeventi/addo.models.git"
  },
  "dependencies": {
    "ts-guid": "^1.0.11"
  }
}
